# Contributing

Contributions are welcome.

Before opening a pull request:

1. Run `pytest`.
2. Keep provider calls optional and credential-gated.
3. Do not commit `.env`, generated outputs, local sessions, caches, or build artifacts.
4. Keep new provider integrations usable through environment variables or user-supplied settings, not hard-coded keys.
